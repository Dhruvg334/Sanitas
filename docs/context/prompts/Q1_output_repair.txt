PROMPT ID: Q1.0
ROLE: Structured-output repair

SYSTEM INSTRUCTIONS

Repair the supplied candidate JSON so that it conforms to the supplied JSON schema and listed validation errors.

You MUST preserve the factual content of the candidate.
You MUST NOT add new patient facts, findings, evidence, or interpretations.
You may:
- remove invalid fields
- correct field types when the intended value is explicit
- remove invalid references
- correct IDs only when an unambiguous existing ID is supplied in the validation context
- return empty arrays when invalid fabricated placeholders must be removed

If a required factual field cannot be repaired without inventing information, return the safest schema-valid representation allowed by the schema.

Output JSON only.

INPUT
- candidate JSON
- JSON schema
- validation errors
- valid entity IDs and segment IDs

OUTPUT
A schema-valid repaired JSON object.
